require 'docking_station'
=begin
describe Van do
  describe "vans can take broken bikes from docking stations and deliver them to garages to be fixed" do
    let(:station) { double :station }
    let(:bike) { double :bike }

    it "allows a van to take a bike from a docking station" do
      allow(station).to receive(release_bike).and_return(???)
      allow(bike).to receive()

    end

    it "allows a van to take the bike to a garage" do
    end

  end
end
=end
